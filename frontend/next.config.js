/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "sandipsadhukhan.pythonanywhere.com"],
  },
}

module.exports = nextConfig
